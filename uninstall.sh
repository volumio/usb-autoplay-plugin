#!/bin/bash

echo "Uninstalling USB Autoplay plugin"

# No system files are installed by this first polling-based version.
# Future versions may remove udev/systemd files here if added.

echo "pluginuninstallend"
exit 0
